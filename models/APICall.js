//key 830ae949e6554aadae74c234f1e467ee
// $("#search-button").on("click", function() {

//     function buildQueryURL() {}
//     // Storing our giphy API URL for a 
//     var queryURL = "https://api.rawg.io/api/games"
//     // Perfoming an AJAX GET request to our queryURL
//     $.ajax({
//       url: queryURL,
//       method: "GET"
//     })

//axios
//axios.get('https://example.com/getSomething', {
//  headers: {
//     User-Agent: 'myappname'
//   }
//  })
  
//     // After the data from the AJAX request comes back
//       .then(function(response) {
//            // Constructing HTML containing the artist information
//       var gameName = $("<h1>").text(response.name);
//       var gameDescription = $("<h1>").text(response.name);
        // var gameRating = $("<h1>").text(response.name)
        //var gameRanking = $("<h1>").text(response.name)


//         event.preventDefault
          
//           // Storing an array of results in the results variable
//           var results = response.data;
  
//           // Looping over every result item
//           for (var i = 0; i < results.length; i++)